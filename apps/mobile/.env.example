# Run `npm run db:start` in the backend (repo root), then `supabase status`
# to print these values for the local stack.
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=
